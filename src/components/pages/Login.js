import StyledSection from "./Page-styles/StyledSection";
import UserForm from "../Form";

const Login = () => {

    return (
    <StyledSection>
        <UserForm/>
    </StyledSection>
    );
};
export default Login;